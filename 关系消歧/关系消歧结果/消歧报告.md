# 关系消歧运行报告

- 输入目录：`D:\work\java_knowledge_graph\关系抽取结果`
- 输出目录：`D:\work\java_knowledge_graph\关系消歧\关系消歧结果`
- 输入关系行数：2138
- 输出规范关系行数：1981
- 关系冲突实体对：147
- 互逆关系方向统一行数：12
- 对称关系端点重排行数：16
- 关系标签规范化行数：12
- 重复规范三元组合并行数：0

## 分文件统计

| 文件 | 输入行 | 输出行 | 冲突实体对 | 方向统一 | 标签规范化 | 重复合并 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| all_relations.csv | 1069 | 990 | 74 | 6 | 6 | 0 |
| cn.csv | 90 | 87 | 3 | 0 | 0 | 0 |
| data_structure.csv | 121 | 106 | 14 | 0 | 0 | 0 |
| java.csv | 195 | 187 | 8 | 0 | 0 | 0 |
| JVM.csv | 411 | 364 | 43 | 2 | 2 | 0 |
| mysql.csv | 21 | 21 | 0 | 0 | 0 | 0 |
| os.csv | 231 | 226 | 5 | 4 | 4 | 0 |

## 冲突消歧示例

### 示例 1

- 实体对：`传输层` -> `TCP`
- 保留关系：`has_part`
- 候选关系：`has_part | used_for`
- 依据：has_part: score=0.784, conf=0.804, support=1, cues=枚举结构；used_for: score=0.721, conf=0.660, support=1, cues=用途,用于,适用于
- 证据：传输层:TCP、UDP、SCTP

### 示例 2

- 实体对：`应用层` -> `HTTP`
- 保留关系：`has_part`
- 候选关系：`has_part | used_for`
- 依据：has_part: score=0.784, conf=0.804, support=1, cues=枚举结构；used_for: score=0.738, conf=0.662, support=1, cues=用途,用于,适用于,\bfor\b
- 证据：应用层:HTTP、HTTPS、FTP、SMTP、POP3、IMAP、Telnet、SSH、DNS、RTP、RTCP、DHCP、SNMP、NTP

### 示例 3

- 实体对：`应用层` -> `SMTP`
- 保留关系：`used_for`
- 候选关系：`used_for | has_part`
- 依据：used_for: score=0.737, conf=0.660, support=1, cues=用途,用于,适用于,\bfor\b；has_part: score=0.728, conf=0.714, support=1, cues=枚举结构；low_margin=0.009
- 证据：应用层:X-Forwarded-For 请求头,简单但易伪造,仅适用于 HTTP/SMTP

### 示例 4

- 实体对：`哈希表` -> `哈希函数`
- 保留关系：`depends_on`
- 候选关系：`depends_on | is_a`
- 依据：depends_on: score=0.791, conf=0.840, support=1, cues=依赖,基于,!用于；is_a: score=0.777, conf=0.850, support=1, cues=上下位,是一种,!用于,!基于；low_margin=0.013
- 证据：哈希表是一种基于哈希函数的数据结构,用于实现高效的查找操作

### 示例 5

- 实体对：`哈希表` -> `数据结构`
- 保留关系：`depends_on`
- 候选关系：`depends_on | is_a`
- 依据：depends_on: score=0.772, conf=0.810, support=1, cues=依赖,基于,!用于；is_a: score=0.759, conf=0.820, support=1, cues=上下位,是一种,!用于,!基于；low_margin=0.013
- 证据：哈希表是一种基于哈希函数的数据结构,用于实现高效的查找操作

### 示例 6

- 实体对：`链表` -> `数据域`
- 保留关系：`is_a`
- 候选关系：`is_a | has_part`
- 依据：is_a: score=0.764, conf=0.784, support=1, cues=上下位,是一种,!包含；has_part: score=0.744, conf=0.714, support=1, cues=整体部分,包含
- 证据：链表是一种链式存储结构,每个节点包含数据域和指针域,适合频繁插入和删除的场景

### 示例 7

- 实体对：`链表` -> `指针域`
- 保留关系：`is_a`
- 候选关系：`is_a | has_part`
- 依据：is_a: score=0.749, conf=0.760, support=1, cues=上下位,是一种,!包含；has_part: score=0.729, conf=0.690, support=1, cues=整体部分,包含
- 证据：链表是一种链式存储结构,每个节点包含数据域和指针域,适合频繁插入和删除的场景

### 示例 8

- 实体对：`哈希表` -> `查找`
- 保留关系：`depends_on`
- 候选关系：`depends_on | used_for | is_a`
- 依据：depends_on: score=0.727, conf=0.738, support=1, cues=依赖,基于,!用于；used_for: score=0.719, conf=0.728, support=1, cues=用途,用于,!基于；is_a: score=0.714, conf=0.748, support=1, cues=上下位,是一种,!用于,!基于；low_margin=0.008
- 证据：哈希表是一种基于哈希函数的数据结构,用于实现高效的查找操作

### 示例 9

- 实体对：`队列` -> `任务调度`
- 保留关系：`used_for`
- 候选关系：`used_for | is_a`
- 依据：used_for: score=0.763, conf=0.728, support=1, cues=用途,用于,常用于；is_a: score=0.741, conf=0.748, support=1, cues=上下位,是一种,!用于
- 证据：队列是一种先进先出(FIFO)的数据结构,常用于任务调度和缓冲区管理

### 示例 10

- 实体对：`线性结构` -> `数组`
- 保留关系：`is_a`
- 候选关系：`is_a | has_part`
- 依据：is_a: score=0.722, conf=0.742, support=1, cues=上下位,!包括；has_part: score=0.718, conf=0.672, support=1, cues=整体部分,包括；low_margin=0.004
- 证据：线性结构是指数据元素之间存在一对一关系的结构,主要包括数组、链表、栈和队列

### 示例 11

- 实体对：`概率型数据结构` -> `误判率`
- 保留关系：`used_for`
- 候选关系：`used_for | is_a`
- 依据：used_for: score=0.734, conf=0.708, support=1, cues=用途,用于；is_a: score=0.729, conf=0.728, support=1, cues=上下位,是一种,!用于；low_margin=0.005
- 证据：布隆过滤器是一种空间效率较高的概率型数据结构,用于判断元素是否存在于集合中,但存在一定的误判率

### 示例 12

- 实体对：`线性结构` -> `链表`
- 保留关系：`is_a`
- 候选关系：`is_a | has_part`
- 依据：is_a: score=0.711, conf=0.724, support=1, cues=上下位,!包括；has_part: score=0.707, conf=0.654, support=1, cues=整体部分,包括；low_margin=0.004
- 证据：线性结构是指数据元素之间存在一对一关系的结构,主要包括数组、链表、栈和队列
